import { AdminPageHeader } from "#/components/admin/common";

export function AdminSettingsHeader() {
	return (
		<AdminPageHeader
			title="Settings"
			description="Configure discovery limits, GitHub search budget, and section behavior."
		/>
	);
}
